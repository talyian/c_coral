c_extern printf: Func[Ptr, ..., Void]

type Foo = {x:Int32, y: Ptr}
func Foo.bar():
  self.x * 3 + 1

func main():
  printf "Tuples Test\n"
  let foo = Foo(3, "frob")
  let a = foo.x
  let b = foo.y
  let c = foo.bar()
  printf("%d -- %s (%d)\n", a, b, c)
  0
