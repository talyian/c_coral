import prelude

type Day:
  | Weekday : Int32
  | Saturday : Void
  | Sunday : Void

let day = Day.Weekday

type Person = {age:Int32}

let person = Person(30)

printf ("Hello, World! %d\n", person.age)
