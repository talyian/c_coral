c_extern printf: Func[Ptr, ..., Void]

# type Foo = {x:Int32, y: Ptr}
# Foo::x  -> (Int32, Index 0)
# Foo::y  -> (Ptr, Index 1)
# Foo::@constructor -> Func[Int32, Ptr, Foo]
# Foo -> Struct
type Bar = Union[x: Int32, y:Ptr, z:Int64]
let b0 = Bar.x 3
let b1 = Bar.y "bar"
printf "hi\n"
# let b1 = Bar.y "bar"
# printf("v: (%d, %s)\n", v0, v1)
