import prelude

type Person = {age:Int32, name:Ptr}
func Person.say():
  printf("hello my name is \"%s\"\n", self.name)
func main():
  let bob = Person(30, "bob")
  printf("person: [%s] (%d)\n", bob.name, bob.age)
  bob.say()
  0
